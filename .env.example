# =============================================================================
# Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!

# -----------------------------------------------------------------------------
# Environment Mode
# -----------------------------------------------------------------------------
# Options: 'development' or 'production'
# Controls image handling, database, and other environment-specific behavior
FLASK_ENV=development

# -----------------------------------------------------------------------------
# Email Service (SendGrid)
# -----------------------------------------------------------------------------
SENDGRID_API_KEY=your_sendgrid_api_key_here
SENDER_EMAIL=your-verified-email@domain.com
SENDER_NAME=Your Business Name

# -----------------------------------------------------------------------------
# SMS Service (Twilio)
# -----------------------------------------------------------------------------
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# -----------------------------------------------------------------------------
# Business Information (Legal Requirement)
# -----------------------------------------------------------------------------
BUSINESS_ADDRESS=123 Main St, City, State 12345

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Development: Use SQLite
DATABASE_URL=sqlite:///database.db

# Production: Use PostgreSQL (Railway provides this automatically)
# DATABASE_URL=postgresql://user:password@host:port/dbname

# -----------------------------------------------------------------------------
# Application URLs
# -----------------------------------------------------------------------------
# Development
BASE_URL=http://localhost:5001
STATIC_URL=http://localhost:5001/static

# Production (Railway)
# BASE_URL=https://your-app.railway.app
# STATIC_URL=https://your-app.railway.app/static

# -----------------------------------------------------------------------------
# Image Handling Strategy
# -----------------------------------------------------------------------------
# Options:
#   - 'base64': Images embedded as base64 (good for development, simple setup)
#   - 'external': Images hosted as external URLs (required for production)
#
# If not set, defaults to:
#   - 'base64' in development
#   - 'external' in production
IMAGE_STRATEGY=base64

# -----------------------------------------------------------------------------
# Security
# -----------------------------------------------------------------------------
# Generate a random secret key for production!
# Python command: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-in-production

# Encryption key for sensitive data (email addresses, phone numbers)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_fernet_encryption_key_here

# =============================================================================
# Development vs Production Settings
# =============================================================================

# DEVELOPMENT SETUP:
# ------------------
# FLASK_ENV=development
# BASE_URL=http://localhost:5001
# DATABASE_URL=sqlite:///database.db
# IMAGE_STRATEGY=base64
#
# Benefits:
# - Simple setup, no external hosting needed
# - Images work immediately (base64)
# - Local SQLite database
# - Debug mode enabled

# PRODUCTION SETUP (Railway):
# ---------------------------
# FLASK_ENV=production
# BASE_URL=https://your-app.railway.app
# DATABASE_URL=<Railway provides this>
# IMAGE_STRATEGY=external
# STATIC_URL=https://your-app.railway.app/static
#
# Benefits:
# - Smaller email sizes (external URLs)
# - Better deliverability
# - PostgreSQL database
# - Production-ready configuration
